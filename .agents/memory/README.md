# Memória persistente — Carbody

Pasta reservada para memória de projeto entre sessões de IA (ex.: papéis com `memory: project` no frontmatter do Claude Code). Vazia até que um papel especializado precise persistir contexto aqui — nenhum dos papéis criados no bootstrap inicial (`three-scene`, `content-catalog`, `react-ui`) declarou essa necessidade ainda.
